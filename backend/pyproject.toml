[project]
name = "dontcave-backend"
version = "0.1.0"
description = "MP3-to-chord-chart personal tool - backend"
requires-python = ">=3.10"
dependencies = [
    "fastapi==0.135.1",
    "uvicorn==0.41.0",
]
